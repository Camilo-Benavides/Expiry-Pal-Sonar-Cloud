# Frontend Vite environment example for ExpiryPal
# Copy this file to .env (Frontend/.env) and fill the values.

# Public Firebase web app config (create a Web App in Firebase console and paste the values)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Backend API URL used by the frontend (adjust if your backend runs on a different host/port)
VITE_API_URL=http://localhost:5000
